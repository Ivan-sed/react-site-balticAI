# Миграция из old site в React

## ✅ Выполнено

### 1. Изображения

Все изображения из папки `old site/images/` успешно перенесены в React проект.

### 2. HTML страницы и CSS стили

Все основные HTML страницы конвертированы в React компоненты с сохранением всех стилей.

## 📁 Структура

```
src/
├── assets/
│   ├── images/           # Все изображения из old site
│   │   ├── index.ts      # Типизированные экспорты всех изображений
│   │   ├── logo.png
│   │   ├── about-image.png
│   │   └── ... (все остальные изображения)
│   └── index.ts          # Главный файл экспорта assets
├── pages/
│   ├── HomePage.tsx      # index.html → React компонент
│   ├── AboutUsPage.tsx   # about-us.html → React компонент
│   ├── ContactsPage.tsx  # contacts.html → React компонент
│   ├── cases/
│   │   └── CaseDetailPage.tsx  # case-page.html → React компонент
│   └── index.ts          # Экспорт всех страниц
├── styles/
│   └── styles.css        # Скопированный CSS из old site
└── components/
    └── ImageGallery.tsx  # Демо компонент с примерами использования
```

## 🔧 Как использовать

### Навигация между страницами

В приложении есть кнопки для переключения между страницами:

- 🏠 Главная (HomePage)
- 👥 О нас (AboutUsPage)
- 📞 Контакты (ContactsPage)
- 📁 Кейс (CaseDetailPage)

### Импорт компонентов

```typescript
import { HomePage, AboutUsPage, ContactsPage, CaseDetailPage } from "./pages";
```

### Импорт изображений

```typescript
import { logo, aboutImage, client1, partner1 } from "../assets";
```

## 📋 Перенесенные страницы

### ✅ Полностью готовые страницы

- **index.html** → `HomePage.tsx` - **ПОЛНОСТЬЮ ПЕРЕНЕСЕНА**

  - ✅ Header с навигацией
  - ✅ Hero секция с заголовком и партнерами
  - ✅ Services секция с карточками услуг
  - ✅ Features секция с описанием преимуществ
  - ✅ Projects секция с примерами работ
  - ✅ Contact секция с призывом к действию
  - ✅ Industries секция с отраслями
  - ✅ Clients секция с логотипами клиентов
  - ✅ About секция со статистикой
  - ✅ Newsletter секция с подпиской
  - ✅ Footer с контактами и ссылками
  - ✅ Все изображения корректно подключены через импорты

- **about-us.html** → `AboutUsPage.tsx` - страница о компании
- **contacts.html** → `ContactsPage.tsx` - страница контактов с формой
- **case-page.html** → `cases/CaseDetailPage.tsx` - детальная страница кейса

### 🎯 Особенности структуры

- **Кейсы в отдельной папке**: `pages/cases/` - для возможности добавления множественных кейсов
- **Типизированные импорты**: TypeScript поддержка для всех ресурсов
- **BEM CSS**: Сохранена оригинальная БЭМ архитектура стилей
- **Адаптивность**: Все стили сохранены включая responsive design

## 🎨 CSS и стили

- **styles.css**: Полностью скопирован из old site (7808 строк)
- **БЭМ методология**: Сохранена оригинальная архитектура
- **Google Fonts**: Подключены Spartan, Kanit, Inter
- **Responsive design**: Все медиа-запросы сохранены

## ⏳ Следующие шаги

1. ✅ Изображения перенесены
2. ✅ HTML страницы конвертированы в React компоненты
3. ✅ CSS стили подключены
4. ⏳ Перенос JavaScript функциональности из script.js
5. ⏳ Добавление React Router для полноценной навигации
6. ⏳ Разделение на переиспользуемые компоненты (Header, Footer, etc.)
7. ⏳ Оптимизация и рефакторинг кода

## 💡 Преимущества React версии

- **Компонентность**: Переиспользуемые React компоненты
- **Типизация**: TypeScript для безопасности типов
- **Hot Reload**: Мгновенные обновления при разработке
- **Модульность**: Четкая структура папок и файлов
- **Масштабируемость**: Легкое добавление новых страниц и кейсов
